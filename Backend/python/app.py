"""
Python Agent Framework Application

A production-ready multi-agent orchestration framework using Microsoft Agent Framework
and FastAPI for enterprise AI applications.
"""

import os
from main import app

if __name__ == "__main__":
    import uvicorn
    
    port = int(os.getenv("PORT", 8000))
    host = os.getenv("HOST", "0.0.0.0")
    
    uvicorn.run(
        "main:app",
        host=host,
        port=port,
        reload=True,
        log_level="info"
    )